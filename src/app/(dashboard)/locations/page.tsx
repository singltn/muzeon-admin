import { LocationsPage } from "@/widgets/locations-page/ui/locations-page";

export const metadata = { title: "Площадки — MUZEON Admin" };

export default function Page() {
  return <LocationsPage />;
}
